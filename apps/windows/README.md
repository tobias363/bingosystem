# apps/windows — Windows-shell (placeholder)

Tom plassholder for Windows-native shell (.exe) som skal pakke `packages/game-client` og web-shell til desktop.

## Planlagt stack

Vurder:
- **Tauri** (Rust + WebView2) — lite footprint, moderne, samme web-UI som mobil
- **Electron** — bredere økosystem, større footprint
- **Unity WebGL-eksport** — kun hvis vi må beholde Unity-rendering (lite sannsynlig)

Beslutning tas når mobil-appene (android/ios) har stabilisert seg.

## Status

**Ikke startet.** Opprettet som plassholder for å reservere mappen i monorepo-strukturen.
