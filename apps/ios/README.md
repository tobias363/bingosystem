# apps/ios — iOS-shell (placeholder)

Tom plassholder for iOS-native shell som skal pakke `packages/game-client` og web-shell til App Store.

## Planlagt stack

- Xcode-prosjekt (Swift + WKWebView eller Capacitor)
- Deler `packages/shared-types` og `packages/game-client` med web og android
- Signering via Apple Developer-konto

## Status

**Ikke startet.** Opprettet som plassholder for å reservere mappen i monorepo-strukturen.
Se `apps/android/` for mønstret som skal følges.
