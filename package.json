{
  "name": "bingo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix backend run dev",
    "check": "npm --prefix backend run check",
    "build": "npm --prefix backend run build",
    "test:compliance": "npm --prefix backend run test:compliance",
    "check:unity": "bash scripts/unity-compile-check.sh",
    "check:all": "npm run check && npm run build && npm run test:compliance && npm run check:unity",
    "deploy:backend": "bash scripts/deploy-backend.sh",
    "build:candy:webgl": "bash scripts/unity-webgl-build.sh",
    "release:candy": "bash scripts/release-candy.sh",
    "release:all": "bash scripts/release-all.sh"
  }
}
